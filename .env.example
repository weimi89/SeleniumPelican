# ═══════════════════════════════════════════════════════════════════════════
# WEDI 宅配通自動化工具 - 環境變數設定範例
# ═══════════════════════════════════════════════════════════════════════════
# 📝 使用方式：
#   1. 複製此檔案並重新命名為 .env
#   2. 根據您的系統取消註解並修改對應的設定值
#   3. 確保 .env 檔案不要提交到版本控制系統（已在 .gitignore 中）
# ═══════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────
# 🌐 Chrome 瀏覽器路徑設定（必要）
# ───────────────────────────────────────────────────────────────────────────
# 用途：指定 Chrome 瀏覽器的執行檔路徑
# 注意：如果不設定，系統將使用預設的 Chrome 安裝路徑

# macOS 預設路徑
# CHROME_BINARY_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"

# Windows 預設路徑
# CHROME_BINARY_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"

# Linux/Ubuntu 常見路徑
# CHROME_BINARY_PATH="/usr/bin/google-chrome"
# CHROME_BINARY_PATH="/usr/bin/chromium-browser"

# ───────────────────────────────────────────────────────────────────────────
# 🚗 ChromeDriver 路徑設定（選用）
# ───────────────────────────────────────────────────────────────────────────
# 用途：指定 ChromeDriver 的執行檔路徑
# 注意：如果不設定，系統將自動嘗試使用系統 ChromeDriver 或 WebDriver Manager

# macOS 範例
# CHROMEDRIVER_PATH="/usr/local/bin/chromedriver"

# Windows 範例
# CHROMEDRIVER_PATH="C:\chromedriver\chromedriver.exe"

# Linux/Ubuntu 範例
# CHROMEDRIVER_PATH="/usr/bin/chromedriver"

# ───────────────────────────────────────────────────────────────────────────
# 🔇 無頭模式設定（選用）
# ───────────────────────────────────────────────────────────────────────────
# 用途：設定瀏覽器是否以無頭模式（背景執行）運行
# 可選值：true（無頭模式，不顯示瀏覽器視窗）/ false（視窗模式，顯示瀏覽器）
# 預設值：true（適合伺服器/自動化環境）
# ⚠️ 警告：無頭模式下無法手動輸入驗證碼！
# 💡 提示：本機開發時可設為 false 以觀察執行過程

# HEADLESS=true

# ───────────────────────────────────────────────────────────────────────────
# 📁 自訂下載目錄設定（選用）
# ───────────────────────────────────────────────────────────────────────────
# 用途：自訂各類型資料的下載目錄
# 預設值：專案根目錄下的 downloads/ 資料夾
# 注意：如果不設定，將使用預設的 downloads/ 目錄

# 代收貨款下載目錄
# PAYMENT_DOWNLOAD_DIR="/path/to/payment/downloads"

# 運費結帳下載目錄
# FREIGHT_DOWNLOAD_DIR="/path/to/freight/downloads"

# 運費未請款下載目錄
# UNPAID_DOWNLOAD_DIR="/path/to/unpaid/downloads"

# ───────────────────────────────────────────────────────────────────────────
# 🐍 Python 環境設定（選用）
# ───────────────────────────────────────────────────────────────────────────
# 用途：控制 Python 輸出緩衝
# 可選值：1（停用緩衝，即時輸出）/ 0（啟用緩衝）
# 建議值：1（確保日誌即時顯示）
# 注意：執行腳本時已自動設定，通常不需要手動配置

# PYTHONUNBUFFERED=1

# ───────────────────────────────────────────────────────────────────────────
# 📊 其他設定（選用）
# ───────────────────────────────────────────────────────────────────────────

# 日誌等級設定
# LOG_LEVEL=INFO

# 瀏覽器等待時間（秒）
# WAIT_TIMEOUT=10

# ═══════════════════════════════════════════════════════════════════════════
# 🔧 快速設定範例
# ═══════════════════════════════════════════════════════════════════════════
# 以下是針對不同作業系統的快速設定範例，請根據您的系統選擇：

# 📱 macOS 快速設定
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
# HEADLESS=false

# 🪟 Windows 快速設定
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="C:\Program Files\Google\Chrome\Application\chrome.exe"
# HEADLESS=false

# 🐧 Linux/Ubuntu 快速設定（伺服器環境）
# ───────────────────────────────────────────────────────────────────────────
# CHROME_BINARY_PATH="/usr/bin/chromium-browser"
# CHROMEDRIVER_PATH="/usr/bin/chromedriver"
# HEADLESS=true  # 伺服器環境建議使用無頭模式

# ═══════════════════════════════════════════════════════════════════════════
# 📚 更多資訊
# ═══════════════════════════════════════════════════════════════════════════
# 完整文檔請參考：README.md 和 CLAUDE.md
# 問題回報：請透過 GitHub Issues 提交
# ═══════════════════════════════════════════════════════════════════════════
